name: loco-manipulation-sdg

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python=3.12
  - pip>=25
  - pip:
      - --extra-index-url https://pypi.nvidia.com
      - "isaacsim[all,extscache]==6.0.1.0"
      - "isaaclab[all]==3.0.0"
      - "pyyaml>=6.0"
